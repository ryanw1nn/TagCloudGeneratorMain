# TagCloudGeneratorMain
Takes in input text file, finds the 'x' most popular words and outputs them into a HTML tag cloud.

input example: data/inputExample.txt
output example: data/outputExample.html
